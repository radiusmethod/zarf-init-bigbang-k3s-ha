name: install-tools
description: "Run Tests"

inputs:
  flavor:
    description: "The flavor of the zarf package"
    required: true
    default: full

runs:
  using: composite
  steps:
    - run: |
        cd zarf/${INPUT_FLAVOR} && zarf dev lint
      shell: bash
