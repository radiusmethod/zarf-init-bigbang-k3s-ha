name: install-tools
description: "Run Tests"

inputs:
  flavor:
    description: "The flavor of the zarf package"
    required: true

runs:
  using: composite
  steps:
    - run: |
        make lint FLAVOR=${{ inputs.flavor }}
      shell: bash
