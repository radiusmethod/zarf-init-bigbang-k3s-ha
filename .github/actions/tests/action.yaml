name: install-tools
description: "Run Tests"

inputs:
  flavor:
    description: "The flavor of the zarf package"
    required: true
    default: full

runs:
  using: composite
  steps:
    - run: |
        cd zarf/${{ github.event.inputs.flavor }} && zarf dev lint
      shell: bash
