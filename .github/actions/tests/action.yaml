name: install-tools
description: "Run Tests for ${{ inputs.flavor }}"

inputs:
  flavor:
    description: "The flavor of the zarf package"
    required: true
    default: full

runs:
  using: composite
  steps:
    - run: |
        cd zarf/${{ inputs.flavor }} && zarf dev lint
      shell: bash
